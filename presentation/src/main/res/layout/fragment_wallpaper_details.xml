<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="viewModel"
            type="com.chignonMignon.wallpapers.presentation.feature.wallpaperDetails.WallpaperDetailsViewModel" />

    </data>

    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:animateLayoutChanges="true"
        android:transitionGroup="true">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <com.google.android.material.appbar.MaterialToolbar
                android:id="@+id/toolbar"
                style="@style/Widget.MaterialComponents.Toolbar.Primary"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:fitsSystemWindows="true"
                app:backgroundColor="@{viewModel.wallpaper.colorPalette.background}"
                app:navigationIcon="@drawable/ic_close"
                app:navigationIconTint="@{viewModel.wallpaper.colorPalette.foreground}"
                app:subtitle="@{viewModel.wallpaper.description}"
                app:title="@{viewModel.wallpaper.name}"
                app:titleTextColor="@{viewModel.wallpaper.colorPalette.foreground}"
                tools:subtitle="Description"
                tools:title="Wallpaper details" />

        </com.google.android.material.appbar.AppBarLayout>

        <ImageView
            android:id="@+id/preview"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:importantForAccessibility="no"
            android:scaleType="centerCrop"
            app:imageUrl="@{viewModel.wallpaper.url}"
            app:layout_behavior="com.google.android.material.appbar.AppBarLayout$ScrollingViewBehavior"
            tools:src="@tools:sample/backgrounds/scenic" />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/dark_overlay"
            android:visibility="@{viewModel.shouldShowLoadingIndicator}">

            <com.google.android.material.progressindicator.CircularProgressIndicator
                android:id="@+id/loading_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:indeterminate="true" />

        </FrameLayout>

        <com.google.android.material.floatingactionbutton.ExtendedFloatingActionButton
            android:id="@+id/floating_action_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="@dimen/double_content_padding"
            android:onClick="@{() -> viewModel.onSetWallpaperButtonPressed(context)}"
            android:text="@string/wallpaper_details_set_wallpaper"
            android:textColor="@{viewModel.wallpaper.colorPalette.foreground}"
            app:icon="@drawable/ic_set_wallpaper"
            app:iconTint="@{viewModel.wallpaper.colorPalette.foreground}"
            app:supportBackgroundTintList="@{viewModel.wallpaper.colorPalette.background}" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

</layout>