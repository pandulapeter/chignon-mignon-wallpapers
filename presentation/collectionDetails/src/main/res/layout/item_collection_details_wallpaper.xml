<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="uiModel"
            type="com.chignonMignon.wallpapers.presentation.collectionDetails.implementation.list.CollectionDetailsListItem.WallpaperUiModel" />
    </data>

    <com.google.android.material.card.MaterialCardView
        style="@style/RoundedCorners"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="@dimen/content_padding"
        android:background="?selectableItemBackground"
        android:clickable="@{!uiModel.wallpaperDestination.url.isEmpty()}"
        android:transitionName="@{uiModel.wallpaperDestination.id}"
        app:cardBackgroundColor="@{uiModel.wallpaperDestination.colorPaletteModel.primary}">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:animateLayoutChanges="true"
            android:paddingBottom="@dimen/content_padding">

            <com.google.android.material.imageview.ShapeableImageView
                android:id="@+id/thumbnail"
                android:layout_width="0dp"
                android:layout_height="0dp"
                android:background="@{uiModel.wallpaperDestination.colorPaletteModel.primary}"
                android:importantForAccessibility="no"
                android:scaleType="centerCrop"
                app:imageUrl="@{uiModel.wallpaperDestination.url}"
                app:layout_constraintDimensionRatio="1.2"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:strokeColor="@color/on_primary"
                app:strokeWidth="@dimen/divider"
                tools:src="@tools:sample/backgrounds/scenic" />

            <com.google.android.material.progressindicator.CircularProgressIndicator
                android:id="@+id/loading_indicator"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:indeterminate="true"
                app:indicatorColor="@{uiModel.wallpaperDestination.colorPaletteModel.secondary}"
                app:layout_constraintBottom_toBottomOf="@id/thumbnail"
                app:layout_constraintEnd_toEndOf="@id/thumbnail"
                app:layout_constraintStart_toStartOf="@id/thumbnail"
                app:layout_constraintTop_toTopOf="@id/thumbnail" />

            <TextView
                android:id="@+id/name"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/content_padding"
                android:layout_marginTop="@dimen/content_padding"
                android:layout_marginEnd="@dimen/content_padding"
                android:ellipsize="end"
                android:gravity="center"
                android:lines="1"
                android:text="@{uiModel.wallpaperDestination.name}"
                android:textAppearance="@style/BodyLargeTextAppearance"
                android:textColor="?colorOnPrimary"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@id/thumbnail"
                tools:text="Title" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </com.google.android.material.card.MaterialCardView>


</layout>